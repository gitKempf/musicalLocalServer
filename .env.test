# Test Environment Configuration

NODE_ENV=test
PORT=17200

# Encryption
ENCRYPTION_KEY_PATH=/tmp/musical-test/keys/encryption.key

# Logging
LOG_LEVEL=error
LOG_DIR=/tmp/musical-test/logs

# Cloud (disabled for tests)
CLOUD_REGISTRATION_ENABLED=false
CLOUD_API_URL=http://localhost:17201

# Database (mock for unit tests)
DB_HOST=localhost
DB_PORT=17202
DB_NAME=musical_test
DB_USER=test
DB_PASSWORD=test

# Claude (mock for tests)
ANTHROPIC_API_KEY=test_key_123
CLAUDE_AUTO_SHUTDOWN_MINUTES=1
